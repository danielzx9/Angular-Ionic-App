import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.danielrivera.parcial1.2',
  appName: 'iparcial1.2',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
